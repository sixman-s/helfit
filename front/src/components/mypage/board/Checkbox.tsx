import s from '../../../styles/mypage/C_Checkbox.module.css';

const Checkbox = () => {
  return (
    <div className={s.boxContainer}>
      <div className={s.innerContainer}>게시물을 등록해주세요!</div>
    </div>
  );
};

export default Checkbox;
